import React from 'react';
import ReactDOM from 'react-dom';

// load the global app styles
import './styles.scss';

import App from './app/app';

ReactDOM.render(<App />, document.getElementById('root'));
